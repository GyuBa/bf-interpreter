#include <stdio.h>
#include <stdint.h>
#include <unistd.h>

int main(int argc, char *argv[]) {
    int8_t opt;

    while((opt = getopt(argc, argv, "vfi")) != -1) {
        switch (opt) {
            case 'v'

            case 'f'

            case 'i'
        }
    }
}
